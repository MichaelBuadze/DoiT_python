შექმენით Flask-ის პროექტი, შემდეგი სტრუქტურით:
/books-app
    /templates
        create_book.html
    /migrations
    app.py
    config.py

ინსტრუქცია:
1. შექმენით მონაცემთა ბაზა lesson34_hw და ცხრილი books შემდეგი ველებით: 
Book:
    id 
    title
    author
    year

2. შექმენით '/book' Flask-ის ენდპოინტი წიგნების დასამატებლად, მომხარებელს უნდა შეეძლოს ფორმის შევსება სადაც შეიყვანს ინფორმაციას და თქვენ შეინახავთ მიღებულ მონაცემებს Book-ის ცხრილში. (შექმენით ასევე create_book.html ფაილი იმისათვის, რომ მომხარებელს შეეძლოს HTML ფორმის შევსება), (GET, POST მოთხოვნა).

3. შექმენით '/update_book/<int:book_id>' Flask-ის ენდპოინტი, სადაც მომხმარებელს შეეძლება ინფომრაციის რედაქტირება.

4. შექმენით '/get_book/<int:book_id>' Flask-ის ენდპოინტი, რომლის საშუალებით იქნება შესაძლებელი კონკრეტული წიგნის ინფომრაციის მიღება (GET მოთხოვნა).

5.შექმენით '/books' Flask-ის ენდპოინტი, რომლის საშულებით იქნება შესაძლებელი ყველა წიგნის ინფორმაციის მიღება (GET მოთხოვნა).

6.შექმენით '/delete_book/<int:book_id>' Flask-ის ენდპოინტი, რომლის საშულებით იქნება შესაძლებელი კონკრეტული წიგნის წაშლა მონაცემთა ბაზიდან (DELETE მოთხოვნა).

დავალება შეგიძლიათ ატვირთოთ დაარქივებული ფაილი სახით.